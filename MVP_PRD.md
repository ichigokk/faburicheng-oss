# 短视频代运营发布日程小程序 MVP

## 目标

为短视频代运营团队管理多个客户的拍摄库存和发布日程。用户通过语音或文字记录某个客户本次拍了几条、每条主题和类型，系统根据客户发布频率、历史内容规律和类型错峰规则，自动排入未来日历；用户也可以继续用自然语言调整排期。

## 核心用户流程

1. 在首页查看日历，按天看到每个客户当天计划发布的视频。
2. 点击底部语音或文字输入，输入类似：
   - “今天给张总拍了 5 条，第一条是老板创业故事，第二条是员工培训，第三条是客户案例，第四条是办公室日常，第五条是招主播。”
   - “把星火娱乐下周三那条客户案例挪到周五，周四换成老板日常。”
3. 系统识别客户、拍摄日期、视频数量、主题、内容类型和用户意图。
4. 新拍摄内容进入该客户素材库，并按客户发布规律自动排到未来空档。
5. 如果某客户未来 2 天没有可发布素材，系统提醒准备文案和拍摄。
6. 如果录入拍摄时客户名未匹配，系统主动询问这是新客户，还是现有客户的另一个称呼；用户选择后再继续记录拍摄和排期。

## MVP 功能范围

### 客户管理

- 新增客户
- 客户名称
- 客户别称：例如用户说“小赵”“赵总”，可绑定到正式客户名
- 发布频率：每天 1 条、每 2 天 1 条、自定义间隔
- 默认发布时段
- 内容类型偏好，例如：案例、干货、老板 IP、日常、招商、产品、口播
- 类型错峰规则，例如：连续 2 条不能都是招商，案例和干货交替

### 新增客户流程

用户可以通过表单或自然语言新增客户。

表单新增时填写：

- 客户名称
- 发布频率
- 默认发布时间
- 主要内容类型
- 是否立即启用排期

自然语言新增示例：

- “新增一个客户，叫星火娱乐，每天发一条，主要发老板 IP、招聘主播、公司日常，默认晚上 8 点发。”
- “给我加一个客户张总装修设计，两天发一条，内容类型是案例、避坑、施工现场。”

LLM 解析后生成结构化客户创建指令，程序校验后创建客户。

示例结构：

```json
{
  "intent": "create_client",
  "client": {
    "name": "星火娱乐",
    "publish_interval_days": 1,
    "default_publish_time": "20:00",
    "content_types": ["老板IP", "招聘", "公司日常"],
    "active": true
  }
}
```

新增客户后，如果同一句话里也录入了拍摄内容，系统应先创建客户，再创建拍摄记录，并根据该客户发布规律自动生成未来排期。

### 拍摄记录

- 拍摄日期
- 客户
- 本次拍摄条数
- 每条视频主题
- 每条视频类型
- 当天日程记录：客户、拍摄条数、大概内容
- 素材状态：待剪辑、待文案、待发布、已发布、作废

录入拍摄内容时，系统需要同时完成两件事：

1. 生成素材记录，并按客户发布规律安排到未来发布排期。
2. 在拍摄当天的日程里生成一条拍摄记录，记录本次服务动作。

当天日程里的拍摄记录示例：

```json
{
  "date": "2026-05-27",
  "type": "shoot_session",
  "client": "星火娱乐",
  "shoot_count": 5,
  "summary": "老板创业故事、员工培训、客户案例、办公室日常、招聘主播"
}
```

### 发布日历

- 月视图 / 周视图
- 每天显示客户和计划发布主题
- 每天同时显示当天拍摄记录，例如“星火娱乐：拍摄 5 条，老板创业故事、员工培训、客户案例”
- 支持查看某客户未来发布队列
- 支持手动拖动或自然语言调整

### AI 输入

第一版建议让 LLM 只做“自然语言解析”，不要让 LLM 直接改数据库。流程如下：

1. 用户输入自然语言。
2. LLM 输出结构化 JSON 指令。
3. 程序校验客户、日期、数量、状态和冲突。
4. 排期引擎执行创建、移动、替换、取消等操作。
5. 返回可确认的结果。

示例结构：

```json
{
  "intent": "record_shoot",
  "client": "星火娱乐",
  "shoot_date": "2026-05-27",
  "items": [
    { "topic": "老板创业故事", "type": "老板IP" },
    { "topic": "员工培训", "type": "日常" },
    { "topic": "客户案例", "type": "案例" }
  ]
}
```

### 自动排期

输入：客户发布频率、未来已排日程、素材库、内容类型错峰规则、历史发布规律。

排期原则：

- 只排到该客户允许发布的日期。
- 优先填补最近的空档。
- 避免同一客户连续发布同类型内容。
- 避免同一天多个客户都发布同质类型时过度集中。
- 优先使用状态为“待发布”的素材。
- 手动调整过的日程锁定，不被自动排期覆盖。

### 提醒

- 每天检查未来 2 天发布计划。
- 如果某客户未来 2 天没有可用素材，提醒“准备文案/拍摄”。
- 如果素材状态卡在“待剪辑”或“待文案”，提醒对应动作。

## 建议数据模型

### clients

- id
- name
- publish_interval_days
- default_publish_time
- content_type_rules
- active

### shoot_sessions

- id
- client_id
- shoot_date
- shoot_count
- summary
- note
- created_at

### video_items

- id
- client_id
- shoot_session_id
- topic
- content_type
- status
- created_at

### publish_slots

- id
- client_id
- video_item_id
- publish_date
- publish_time
- status
- locked
- source：auto / manual / ai_adjusted

### reminders

- id
- client_id
- remind_at
- type
- message
- resolved

## 技术建议

### 小程序端

- 首页：日历 + 底部语音/文字输入
- 客户页：客户发布规律、内容类型规则、素材库存
- 新增客户页：客户名称、发布频率、发布时间、内容类型规则
- 日程详情页：当天计划、状态调整、手动改期
- 拍摄记录页：历史拍摄批次

### 后端

- API 服务负责客户、素材、日程、提醒
- 排期引擎独立成服务函数，方便测试
- LLM 调用层只返回结构化指令，业务修改由后端完成

### LLM 能力边界

LLM 适合做：

- 识别客户名
- 识别主题列表
- 识别内容类型
- 识别调整意图
- 把自然语言转为结构化命令

LLM 接入策略：

- 主模型：DeepSeek。
- 备用模型：Qwen，使用阿里云百炼 DashScope OpenAI 兼容接口。
- API key 只放在本地服务端环境变量，不写入小程序前端。
- 前端调用本地后端解析接口，后端先请求 DeepSeek；DeepSeek 失败、超时或未配置时，再请求 Qwen。
- 如果两个模型都不可用，第一版原型保留本地规则解析兜底。

LLM 不应该直接做：

- 直接写数据库
- 绕过排期规则
- 覆盖用户锁定日程
- 在日期不明确时擅自猜太远

## 第一版验收标准

- 能创建 10 个以上客户，并配置不同发布频率。
- 能通过表单新增客户。
- 能通过自然语言新增客户。
- 能用文字输入记录一次拍摄，并自动生成多条素材。
- 新素材能按频率自动进入未来日历。
- 日历能显示每天每个客户的发布计划。
- 能用自然语言移动、替换、取消某条发布计划。
- 客户未来 2 天缺素材时能生成提醒。

## 后续增强

- 接入微信语音识别或第三方 ASR。
- 根据历史爆款类型提高某类内容权重。
- 加入剪辑、文案、审核、发布的团队协作状态。
- 支持导出客户周报/月报。
- 支持和飞书、企微或微信服务通知联动。
