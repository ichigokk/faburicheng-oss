# 贡献指南

感谢你参与改进短视频排期助手。

## 提交问题

- Bug 请描述复现步骤、预期行为、实际行为和运行环境。
- 新功能请先说明目标用户、使用场景和期望结果。
- 不要在 Issue、截图或日志中提交 API key、令牌、客户数据或其他隐私信息。

## 本地开发

```bash
cp .env.example .env
npm start
```

提交前运行：

```bash
npm run check
git diff --check
```

## Pull Request

- 每个 PR 只解决一个明确问题。
- 说明改动原因、用户影响和验证方式。
- 涉及 UI 时，请附截图或录屏。
- 涉及状态迁移、认证或模型指令时，请补充回归说明。

## 开发约定

- 不要提交 `.env`、`data/`、`cloud-env.json` 或微信开发者私有配置。
- 保持 Node.js 20 兼容。
- 修改微信小程序核心逻辑时，注意同步检查 Web 端对应行为。
